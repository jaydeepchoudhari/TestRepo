# reference repo: https://bitbucketp.duke-energy.com/projects/PET/repos/helm-pcf-compat/browse
config-server-compat:
  enabled: false
  vault:
    enabled: false

healthcheck:
  type: "tcp" # process|http|tcp.
#   path: "/" # optional health check path, if http (e.g. /actuator/health).


ingress:
  hosts:
    - host: forms.duke-energy.com
      paths:
        - path: / 
          pathType: ImplementationSpecific
  tls:
    - secretName: tls-forms.duke-energy.com
      host:
        - forms.duke-energy.com       
    
resources:
  requests:
    # cpu: 250m
    memory: 1024Mi
  limits:
    # cpu: 500m
    memory: 1024Mi
