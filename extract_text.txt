RESPONSE=$(curl -s --request POST --data "{\"password\": \"$VAULT_USER_PWD\"}" "$VAULT_ADDR/v1/auth/ldap/login/$VAULT_USER")

echo "Extracting token..."
          TOKEN=$(echo "$RESPONSE" | jq -r '.auth.client_token // empty')

          if [ -z "$TOKEN" ]; then
            echo "ERROR: Failed to retrieve Vault token. Response:"
            echo "$RESPONSE"
            exit 1
          fi

          echo "Token retrieved successfully"
